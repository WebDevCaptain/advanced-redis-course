
Working with JSON arrays

In this movie, let's see how we can specify JSON values that are arrays. Here I use JSON.SET once again to set the value for the user's key. Once again, I have the dollar sign indicating the root path for our new key and you can see that the value here is just an array of JSON objects. Observe that the JSON value is specified within square brackets and we have a number of different JSON objects within those square brackets. That's what makes this an array. If you want to know what type of data you've set for your users key, use JSON.TYPE and the type will make it very clear that what you have here is an array. So it's not a single JSON object, it's an array. Let's do a JSON.GET on the user's key and this will give you the entire array of JSON objects associated with this key. If you want to view your JSON value using the Redis serialization protocol or RESP format, use JSON.RESP and here you can see the multiple objects within the JSON array using the RESP format. Observe that we have four different objects in our array, one for Gerald, one for Naziya, one that is nil and another that's true. Array objects can be looked up based on their index as well. Use JSON.GET $ . and in square brackets the index value will give us the object at that specified index. Here is the object at index 0. Let's understand the JSON path a little more. You don't really need the dot, when you work with arrays, you can simply specify $ and square brackets and you can use negative indices as well. This will give us the last element in the array that's associated with the user's key. Let's try this once again with a negative index, $ and in square brackets, [-3] will give us the third to last element, which is just the second element in our array. You can also access a specific field for a particular element in your JSON array. Here I've specified JSON.GET on users and my JSON path that starts with a $ indicates that I'm looking for the value of the name field for array element at index 0 and that gives me the name Gerald. You can access the individual fields for different elements in a JSON array using a single JSON.GET. Here, I access the name field for the elements at index 0 and 1. And this gives me Gerald and Naziya. Let's say I want the names of all of the objects in my JSON array. I can use a wildcard for the index as well. The asterisk or the star within square brackets will match all elements of the array and for all elements, we'll access the name field and that will give us Gerald and Naziya once again. Another way to access the same attribute or the same field in all nested objects within an array is to use the dot dot. So $ . . Name will give me the name field for all nested objects in my array. Again, Gerald and Naziya. It's possible to delete a particular attribute for a particular element in your JSON array. Your $ . [1]. Name will delete the name attribute for the object at Index 1. Let's check this by doing a JSON.GET for users at index 1 and you can see the name here has disappeared. This was Naziya. You can get rid of an entire element in the JSON array here. JSON.DEL users $ . and in square brackets, [0] will get rid of the element at index 0. Let's do a JSON.GET to confirm this deletion. JSON get users will now show us that we have one object with the location Bangladesh and null and true. Only three elements in the array rather than four. Gerald has disappeared and of course you can delete the entire key value pair as well. JSON.DEL users will get rid of the value associated with the user's key and you can confirm this with a JSON.GET users and this will return nil. Let's consider another JSON array example. This time the elements or objects in an array will be slightly more complex. I use JSON.SET to set the value for the user: admins key. Now this array will be set to the JSON root path as you can see from the $ in the command, The value here is an array of JSON objects as specified by the open square brackets and the close square brackets. Now every element represents a single user. Now let's look at one of the elements. The name of the user is Naziya, and then we have the mailing address of the user. And the value of that is a nested object with a work email and a personal email. Thus, the objects in the array themselves contain nested objects within them. Now, how would we work with this? Let's first take a look at the data using JSON.RESP in the Redis serialization protocol. Consider the object at Number3, the user Naziya, that in itself is a JSON object. You can see from the open curly brace at 0.1 inside Number 3 and observe that the value for the mail attribute is another nested object. You can see the open curly brace for just under the mail property for Naziya, there is a work email address for Naziya and a personal email address for Naziya. Gerald and Naziya have work and personal email addresses, but with Steffi, it's a little different. Steffi has an attribute called contact and has a phone number for work and personal and an email address just for work. Let's look at the JSON path to access all of the personal email addresses for all objects in the array, all users. Start at the root JSON path, that's the $, then the dot and the square brackets and the wildcard asterisk within square brackets that will match all elements in the JSON array. Then we access the nested field mail and the nested field personal within the mail nested object. And here you can see gerald.home@loonycorn.com and naz.home@loonycorn.com. Those are the personal email addresses for the user admins who have them. A slightly different query would be this one. If you want the value of the personal attributes for all nested objects, you can say $ . . Personal. Now notice that we have three entries in the result. Two of them are email addresses and one of them is a phone number. This is a recursive search for all nested objects that have the personal attribute and the corresponding values are returned. I'll now perform the same recursive nested search, but this time using JSON.RESP and you see two email addresses and one phone number in the result.




Performing operations on JSON arrays

Let's explore some of the operations that you can perform on JSON arrays in Redis. First, I'm going to set up a key whose value is an array type. user: mods is the key, that is, this contains a list of moderators for our site. Initially, the only moderator for our site is Ivan, located in Bulgaria. Let's confirm that the array has been set correctly for user: mods. Let's do a JSON.GET and you can see that this array has exactly one element, the object for Ivan. Now, if you want to add a new element to this array, you can use the JSON.ARRAPPEND command. Append to user: mods, another element in the array for Jack located in China. Now if you do a JSON.GET user: mods, you'll find that our array has two elements. We have Ivan in Bulgaria and Jack in China. The JSON.ARRAPPEND command can be used to append multiple elements to your array. Here I'm appending two different elements. The first element is for Arturo, located in Argentina, and the second element is for Dayo, located in Nigeria. If you now look at the user: mods array using the rRESP format, you'll find that we have four different elements Ivan, Jack, Arturo, and Dayo. You can query the length of a JSON array in Redis using the JSON. ARRLEN command. This will tell us that there are four elements in this array. The array has a length of four. If you want to pop or remove one element in the array at a specified index, you can use JSON.ARRPOP. We now pop the element at index zero. The element that was popped and removed from the array is Ivan's record. You can confirm the removal or the popping of this element by looking at user: mods using JSON.RESP. Observe that there are just three elements here. Ivan has been popped or removed from the array. If you run the JSON.ARRPOP command on an array and do not specify an index by default, the last element in your array will be popped and removed from the array. This happens to be due from Nigeria. Once again, we'll run a JSON.RESP user: mods to confirm that Dayo has been removed and we now have just two elements in the array, for Jack and Arturo. The JSON.ARRINSERT command can be used to insert an object as a specified location. Here I want to insert the object for Dayo at location 0. Notice the 0 just after the $. If you look at the contents of the array using JSON.RESP, you'll find that Dayo is at the very first position of the array at Index 0 because that's where we had inserted this object. If you want to insert at a position from the end of the array, you can use negative indices as well. Notice JSON. ARRINSERT used with -1. This means that Ivan will be inserted at the last but one position at the end of the array, that is, the second to last position at the end of the array. And we can confirm this by running JSON.RESP user: mods. And you can see here that Ivan is at the last but one position, that is, the second to last position.
