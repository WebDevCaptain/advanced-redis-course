
Comparing latencies for Redis servers in different locations

Here, I'm with the terminal window of my local machine open. Remember, I have the Redis server installed on this machine. That means I have access to the Redis benchmarking utility. I'm now going to run my benchmark tests on the Redis servers that I have running in India and in the US. Now, we'll connect to the Redis server running on our cloud VMs using the VMs IP address. I'm going to set up an environment variable called REDIS_INDIA with the IP address of my machine that is running the Redis server in India. I'll configure another environment variable, REDIS_USA, containing the IP address of my Redis server located in the US. I'll just use the echo command to confirm that both of these environment variables have been set up correctly. Here is REDIS_INDIA pointing to the IP address of my cloud VM. Yours will be a little different, mind you. And here is the REDIS_USA environment variable pointing to the IP address of my cloud VM located in the US. I'll run my first benchmark test against my Redis server in India; redis-benchmark -h, and then the IP address of my India machine. The port on which the Redis server is running is 6379. I'm going to make 10,000 requests for the different commands that I want to benchmark, and the commands that I'm going to benchmark are just SET and GET. You'll also need to specify the password for the Redis server, which in this case happens to be redis-instance-india-password. When you run these tests, you'll get statistics for both the SET as well as the GET operation. Let's just take a look at the SET. Ten thousand requests were completed in about 5.91 seconds. If you scroll down and look at a summary of the throughput, you can see that about 1690.62 requests were made per second, and the median latency was 29.055 milliseconds. I'll leave it to you to take a look at the statistics for the GET command. Keep these statistics in mind. Now, if you remember when we ran these tests against our local machine, our average latencies were of the order of 0.5 milliseconds. Here the latencies are higher because we are running these against a Redis server hosted on the cloud. I should tell you that I'm recording this course in India, which means my local machine is also in India. Now that said, let's run these benchmark tests against the Redis server that is located in the US. Notice my -h REDIS_USA, and my password is redis-instance-usa-password. I'll be executing SET and GET commands each 10,000 times. Go ahead and run these tests, and let's take a look at the SET latencies and throughput. Ten thousand SET requests for completed, but this took 49.11 seconds. Now, for the same 10,000 requests made against the Redis server located in India, we took just 5.91 seconds. Now let's take a look at the throughput of the SET operation. You can see that we made 203.64 requests per second. If you compare this with the server that's located close by, the throughput there was much higher, 1690.62 requests per second. The median latency for the US server is 235.007 millisecond, for the India server was just 29.055 milliseconds. It's pretty clear here that if your Redis server is located closer to where your clients are running, you're going to get better performance. By default, the benchmark tests are run using 50 parallel connections. If you want to increase the number of concurrent connections, you can use the -c flag as I've done here. Here, I run the benchmark test on my India server with 100 concurrent connections. If you run these tests and scroll up to take a look at the results, notice that there are 100 parallel clients running. And now run the same test with 100 parallel connections on my server based in the US. Notice the -c 100 flag. I'll leave it to you to interpret the results of these two benchmark tests against the US server and the Indian server, but you'll find the conclusion is the same. The server that's close by has lower latency and better throughput. No real surprises there. By default, the benchmark tests that run GET and SET operations, do so using the same key for all GET and all SET operations. If you want to run your test using randomly generated keys, so not all the same key, you can use the -r option. -r 1,000 will generate 1,000 different random keys that will be used for the benchmark test that I run. Go ahead and run these tests, and let's take a look at the output. The output is in the csv format, thanks to the --csv flag. For both the SET and GET operations, the number of requests per second is roughly 1690, you can see the second column in the output, and the median latency is around 28 milliseconds. Let's run the same test with random keys on our server based in the US. Notice the -r 1,000 flag, this is the US server, and let's take a look at the results in the csv format. Here, for both the SET and the GET operations, the number of requests per second is roughly 200, 204, and 201 respectively, and the median latency is around 235 milliseconds.
