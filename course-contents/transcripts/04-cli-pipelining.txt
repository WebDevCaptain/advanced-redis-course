
Pipelining using the Redis CLI

In this movie, we'll see how we can optimize round trip times to a Redis server by batching Redis commands using pipelining. This is the first tab in my terminal window, and I'm going to use this to run the Redis server. So I run redis-server and the server will be up and running here. Now I'm going to open up another terminal window, and I'm going to set up the Redis client here. I run redis-cli to connect to my Redis database server. And I'm going to open up one more terminal window and this will be in my current working directory. This is where I'll run all of my directory and folder specific commands. The first thing that I'm going to do here is make a folder called advancedredis & CD into that folder. Now behind the scenes, I've added a file to this folder called pipeline_commands.txt. So run an ls -l and you can see pipeline_commands.txt in here. If you open up this file in any kind of local editor, you'll find that it contains a number of commands that will run as a batch. You can see a number of SET operations on different keys here and a number of HSET operations as well. There are 10 different commands here and I want to execute these commands in a pipeline using the Redis client. If you're working on a machine with the Redis client installed and correctly set up, this is very simple. All I do here is run a cat command on pipeline_commands.txt and pipe that output to the redis-cli --pipe. This will essentially batch all of the commands in the text file, send it to my Redis database server. The commands will be executed on that server, and I'll get a response. There were 10 different commands and with one round trip I got all 10 responses, and there were no errors. Let's confirm this. I'm going to switch over to the tab that I have, the redis-cli, where I've logged in to the redis-cli and I'm going to run a GET operation for the key 01 and you can see that it has a valid value. Clearly, our commands run as a pipeline were executed successfully. Let's run another GET operation for the key 05 and this has a value as well. Let's confirm that the name property for our key 100 has been set correctly and you can see that this returns "Nora."Aand I'll do a last command here, HGETALL for the key 100 and this will return the entire list of properties associated with this key. Well, we've seen that it was very simple and straightforward to run a batch of commands using a pipeline if you have the REDIS CLI installed on your machine.
